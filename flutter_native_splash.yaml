# Regenerate through `dart run tool/generate_native_branding.dart` so Android
# density resources are deduplicated after the native splash is created.
flutter_native_splash:
  color: "#030206"
  android: true
  ios: true
  web: false

  android_12:
    color: "#030206"
    image: assets/branding/app_icon.png
